
/*Abstract Data Type(ADT)

1.What is ADT?  -->> related to OOP lang.                    //Abstract -> Hiding internal details //Defn of Datatype -> 1.Representation of Data. 
                                                                                                                         2.Operation on Data.

2.List ADT

Data --> 1.Space for storing element.
         2.Capacity.(max. total length)
         3.Size (no. of elements it have)

List ->1.Array
       2.Linked List

operations: add(x)
            remove()
            search(key)  

~add(element)/append(element) -> adding an element at the end of the list.
~add(index,ele)/Insert(index,element)
~remove(index)->removing elements and occupying the vacant space.
~set(index,element)/Replace(index,element)->changing an element in a given index.
~get(index)->knowing an element at a given index.
~search(key)/contains(key)->result will be the index of the element searched.
~sort()

*/